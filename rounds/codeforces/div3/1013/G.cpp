#include <bits/stdc++.h>
using namespace std;

void solve() {
    int s, k;
    cin >> s >> k;

    if (s % k == 0) {
        cout << k << "\n";
        return;
    }

    
}

int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0);

    int t;
    cin >> t;
    while (t--) solve();
}
